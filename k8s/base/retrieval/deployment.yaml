apiVersion: v1
kind: Secret
metadata:
  name: retrieval-secret
  namespace: futbot
type: Opaque
stringData:
  QDRANT_URL: http://qdrant:6333
  QDRANT_COLLECTION: futbot_chunks
  DATA_DIR: /data

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: retrieval
  namespace: futbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retrieval
  template:
    metadata:
      labels:
        app: retrieval
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8085"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: retrieval
          image: futbot-retrieval:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          envFrom:
            - secretRef:
                name: retrieval-secret
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4317"
          volumeMounts:
            - name: retrieval-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3
      volumes:
        - name: retrieval-data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: retrieval
  namespace: futbot
spec:
  selector:
    app: retrieval
  ports:
    - port: 8085
      targetPort: 8085
